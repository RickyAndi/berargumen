const isAuthenticated = require('./is-authenticated');
const isNotAuthenticated = require('./is-not-authenticated');
const apiIsAuthenticated = require('./api-is-authenticated');

module.exports = {
  isAuthenticated,
  isNotAuthenticated,
  apiIsAuthenticated
};